// Shared types used by multiple Member panel components. Types used only by a
// single component are defined in that component's file.

export type TabId = "home" | "schedule" | "absen" | "bills" | "leave" | "rapor" | "profile";
